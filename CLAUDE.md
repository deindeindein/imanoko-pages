# CLAUDE.md

`imanoko-pages` は、iOS アプリ「いまのこ」の公開ページを置くリポジトリです。
GitHub Pages（https://deindeindein.github.io/imanoko-pages/）でそのまま配信されます。

このファイルは、他リポジトリの `AGENTS.md` / `CONTRIBUTING.md` と、これまでのコミットで
繰り返し指摘されている点をまとめたものです。以下を守って作業してください。

## このリポジトリの構成

- 静的 HTML 5枚だけ。ビルドもパッケージ管理もない。編集した HTML がそのまま本番になる。
  - `index.html` … 入口。全ページへのリンク
  - `privacy-policy.html` … プライバシーポリシー
  - `terms.html` … 利用規約
  - `tokusho.html` … 特定商取引法に基づく表記
  - `support.html` … サポート（問い合わせ先と FAQ）
- CSS は各ファイル内の `<style>` に直書き。外部 CSS・JS・フォント・CDN は使わない。
- `terms.html` と `tokusho.html` は **アプリの設定画面から直接開かれる**。
  ファイル名を変えたり消したりすると、アプリ側が 404 になる。リネームは不可。

## 書くとき

- **日本語で書く。** ページ本文、コミットメッセージ、PR、やり取りすべて日本語。
- **実装で確認できたことだけ書く。** 動作、画面遷移、保存先、期間、上限などを推測で書かない。
  確認できないときは、書かずに何が確認できなかったかを伝える。
  - 過去の事故: プライバシーポリシーに「iOS の共有画面を開くだけ」と書いてあったが、
    実際はメールの下書きが開く挙動だった（コミット `10054e3`）。
- **読み手は保護者。** 専門用語を避け、短い文で書く。結論を先に、太字は要点のみ。
- 既存ページの語り口・見出しの粒度・`<strong>` の使い方に合わせる。新しい書式を持ち込まない。
- 数字と条件（7日以内、最大8個、市区町村まで、番地は表示しない等）は仕様そのもの。
  変更するときは必ず確認を取る。勝手に丸めたり言い換えたりしない。

## 直したとき

- **本文を直したら、そのページの「最終更新日」も直す。** `privacy-policy.html` と `terms.html` には
  「更新したら最終更新日を書き換える」と明記してある。
- **リンクを追加・変更したら、リンク先が存在するか確認する。**
  - 過去の事故: アプリの設定画面から開く `terms.html` と `tokusho.html` がリポジトリに無く、404 だった（コミット `10054e3`）。
- 4ページのいずれかの記述を変えたら、他ページに同じ話題の記述がないか `grep` して食い違いを残さない
  （問い合わせ先、保存先、削除時の挙動、返金の窓口は複数ページに出てくる）。

## コミットとブランチ

- コミットメッセージは日本語。1行目に何をしたか、空行のあと **なぜ直したか・何が問題だったか** を書く。
  ここが後から見返す唯一の記録になる。
- ブランチ名は `feature/…` `fix/…` `docs/…`（`refactor/…`）。
- 重要な判断（表記を変えた理由、法務上の判断、アプリ側との取り決め）はコミット本文に残す。

## やらないこと

- URL、会社名、連絡先、公開日、価格、法令名を捏造しない。確認できたものだけ載せる。
- 個人情報（メールアドレス以外の連絡先、住所の番地など）を新たにページへ書き足さない。
- 見た目のリファクタリング、フレームワーク導入、ファイル分割を頼まれずに行わない。

---

うまくいかなかったことがあったら、その都度この文書に1行足してください。
